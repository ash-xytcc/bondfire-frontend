# Bondfire production client configuration.
# API requests use same-origin Pages Functions by default.
# Set VITE_API_BASE_URL only for a deployment that intentionally hosts the API on a separate origin.
VITE_API_BASE_URL=
